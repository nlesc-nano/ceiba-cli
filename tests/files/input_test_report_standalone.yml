# Path to the workdir with the jobs
path_results: "tests/files/mocked_standalone"

# The data is not associated with a job
is_standalone: True

# Pattern to search for the result files
output: "result*csv"