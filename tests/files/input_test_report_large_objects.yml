# Path to the workdir with the jobs
path_results: "tests/files/workdir_ceibacli"

# Pattern to search for the result files
output: "result*csv"

# Large files to save in the remote storage
large_objects:
    web: "http://127.0.0.1:8080/auth/v1.0"
    patterns: ["*.npy"]

collection_name: examples